{
  "name": "health-twin-backend",
  "version": "1.0.0",
  "description": "Backend services for Health Twin™ Platform",
  "private": true,
  "workspaces": [
    "services/*",
    "shared/*",
    "api-gateway"
  ],
  "scripts": {
    "start": "npm run start --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0",
    "nodemon": "^3.0.0"
  }
}
